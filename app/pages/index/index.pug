extends ../../layouts/default

block head
  - var pageTitle = 'Versta Project Template'

block content
  +b.index
    +bg
    +grid
      +col(4)
        +logo
        +title
      +col(4)
        +contacts
        +content
