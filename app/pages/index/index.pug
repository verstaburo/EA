extends ../../layouts/default

block head
  - var pageTitle = 'Фонд Сердце ЕврАзии'

block content
  +b.index
    +bg
    +grid
      +col(4)
        +logo
        +title
      +col(4)
        +contacts
        +content
